fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.27.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Supabase (replaces MySQL RDS + S3 + PGVector)
supabase==2.4.6

# Redis via Upstash REST (free, no Redis server needed)
upstash-redis==1.1.0

# PDF parsing & text chunking
pypdf==4.2.0
python-docx==1.1.2

# Embeddings (local, 100% free, no API needed)
sentence-transformers==3.0.0
torch==2.3.0
numpy==1.26.4

# LLM via Groq (free tier)
groq==0.9.0

# Utilities
aiofiles==23.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Tier 3 College Web Automation
selenium==4.15.2
beautifulsoup4==4.12.2
webdriver-manager==4.0.1
google-search-results==2.4.2
